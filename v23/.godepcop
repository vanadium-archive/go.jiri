<godepcop>
  <!-- The v23 tool is the most critical tool in devtools since it's used for
       bootstrapping the codebase.

       Ensure the strict rule of only allowing dependencies on devtools itself
       and v.io/x/lib, to reduce the exposure to breakage.  Technically this
       isn't necessary since v.io/x/devtools/.godepcop already provides the same
       guarantee, but it's better to be safe than sorry.  -->
  <import allow="v.io/x/devtools/..."/>
  <import allow="v.io/x/lib/..."/>
  <import deny="..."/>
</godepcop>
